import React from 'react';
import {
    Layout,
} from 'antd';

import './automata.css';

export const Automata: React.FC = () => {

    return (
        <Layout>
            <h1>Automata</h1>
        </Layout>
    );
}